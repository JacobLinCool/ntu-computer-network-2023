#ifndef _GUARD_HEADER_MIDDLEWARE_LOGGER
#define _GUARD_HEADER_MIDDLEWARE_LOGGER

#include <time.h>

#include "../middleware.h"

bool middleware_logger_handler(Request* request, Response* response);

#endif
